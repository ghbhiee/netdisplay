# Windows 端（Receiver 已完成，Sender 待做）

Electron + WebCodecs。由 Windows 端 Claude 维护。
代码待 Windows 端首次 push 到此目录（含 src/、main.js、tools/、package.json 等）。

启动：`npm install && npm start`；打包：`npm run dist`（portable .exe）。
