# NetDisplay 界面设计（统一版）

> 两版被否后重做。用户原话：「完全看不懂，怎么又出来一个自动」「更扯淡，我到底怎么启用中转？
> 如果我用中转，还要设置本机地址吗？」——**问题不在实现，在我们把实现细节当成了用户的选择题。**
> 本文定稿后两端统一照此实现。

## 一、两版失败的共同根因

| 版本 | 做法 | 为什么错 |
|---|---|---|
| v1 | `连接方式：自动 / 直连 / 中转` | **「自动」的存在本身就是证据**——我们自己也知道用户答不上「该选直连还是中转」，于是加第三个选项去掩盖，结果三个都要理解 |
| v2 | 去掉模式，改「监听 / 连接」，两个字段都填就并行竞速 | 用户看到「配对码」和「本机地址」两个框并列，**不知道该填哪个、为什么要填两个**；跨网络用中转时还在显示对方根本连不上的本机局域网地址 |

**根因：把 transport（直连/中转）当成了「用户要做的选择」。**
它其实是**程序自己能算出来的状态**——而且用户根本判断不了：我们自己都被 CGNAT 的相同出口 IP 骗过，以为两台机器同网。**既然我们都会判断错，就不该要求用户判断。**

## 二、设计原则

1. **用户只需理解一个概念：配对码。** 没有「直连/中转/自动」，没有 IP 输入框，没有模式选择。
2. **transport 是状态，不是选项。** 界面只显示「直连 3ms」或「中转 310ms」——这是信息，不是让人选的。
3. **参数就近、随时可改**，不必断开重连去翻设置。
4. **主界面永远一屏**，不滚动。高级项折叠。

参照 TeamViewer / AnyDesk / Parsec：它们都同时支持 P2P 与中转，**界面上从来没有这两个词**，用户只交换一个 ID。

## 三、界面

### 状态 1：未连接（启动即此屏）

```
┌────────────────────────────────────┐
│  ▣ NetDisplay                      │
│                                    │
│  我的配对码                         │
│  ┌──────────────────────────────┐  │
│  │   4 8 1 9 2 0        [换一个] │  │
│  └──────────────────────────────┘  │
│  把它告诉对方，对方输入后即可连上     │
│                                    │
│  ──────────── 或 ────────────      │
│                                    │
│  连接对方                           │
│  ┌──────────────────────────────┐  │
│  │  输入对方的配对码              │  │
│  └──────────────────────────────┘  │
│         [    连  接    ]           │
│                                    │
│  ▸ 高级设置                        │
└────────────────────────────────────┘
```

**没有模式选择。** 本机**始终**在等对方输入我的码（后台常驻），同时你也可以输入对方的码去连。谁先动谁发起——这正是 AnyDesk 的模型，用户从不需要想「我该当哪一方」。

### 状态 2：已连接 · 待命

```
┌────────────────────────────────────┐
│  ▣ NetDisplay                      │
│  ● 已连接 MacBook-Pro               │
│    直连 · 3ms                       │  ← transport 在这里，是状态
│                                    │
│  ┌──────────────────────────────┐  │
│  │      ▶  投射本机画面          │  │
│  └──────────────────────────────┘  │
│  投射源  [ 整个屏幕            ▾ ]  │
│                                    │
│  画质  [2560×1600 ▾] [2x ▾] [60 ▾] │
│                                    │
│              [ 断开 ]              │
│  ▸ 高级设置                        │
└────────────────────────────────────┘
```

对方一开始投射，自动进入状态 3——不需要在这里点「接收」。

### 状态 3：正在显示对方画面

全屏画面 + 顶部悬浮条（鼠标移动才出现，2.5s 淡出）：
```
   [⏏ 弹回对方]  [■ 停止]  [⚙]        右上角: 2560×1600 hevc422 58fps 3ms
```

### 高级设置（折叠，默认不展开）

```
中转服务器   [15.tokencv.com:47700    ]
访问令牌     [                        ]
码率上限     [ 40 ] Mbps
□ 强制走中转（企业网络禁 P2P 时用）
□ 窗口显示（不全屏）
```

**局域网地址不再作为输入项存在**——它由程序在握手时自动交换（见下）。

## 四、底层：transport 怎么变成「状态」

```
1. 双方都连中转（一定能通）→ 撮合 → 建立会话
2. HELLO 里互相告知各自的局域网地址（新增可选字段 lanAddrs）
3. 后台并行尝试直连对方的 lanAddrs
4. 直连握手成功 → 无缝切换到直连，状态栏改显「直连 3ms」
5. 直连不通 → 继续用中转，状态栏显「中转 310ms」
```

- **升级只在待命时做**，投射中不切链路（避免画面中断）。
- 直连监听端口**始终开着**，不再依赖「监听模式」。
- 协议改动很小：HELLO 加可选 `lanAddrs`，靠 §0「容忍未知字段」规则天然向后兼容。

## 五、技术栈：换掉 Electron

用户要求不用 Electron。本机实测：**未装 .NET SDK**，但 **Rust 1.92 + cargo + MSVC 齐全**。

| 方案 | 体积 | 原生度 | 视频管线 | 评估 |
|---|---|---|---|---|
| Electron（现状） | 71 MB | 低 | WebCodecs 现成 | 用户已否 |
| **Tauri（Rust + WebView2）** | **~5 MB** | 中高 | **WebCodecs 可复用** | **推荐** |
| Rust + windows-rs 纯 Win32 | ~3 MB | 最高 | **要全部重写**（MF 编解码 + WGC 采集 + D3D 渲染） | 工作量最大 |
| C# WinUI/WPF | ~10 MB+ | 最高 | 同样要全部重写 | 还得先装 .NET SDK |

### ⚠️ 实测结果推翻了「Tauri 保留现有管线」这个前提

推荐 Tauri 的核心理由本是「WebView2 也有 WebCodecs，管线可原样保留」。**实测不成立**
（`tools/probe-webview2.js`，同一台机器同一天）：

| 引擎 | H.264 | HEVC 4:2:0 | HEVC Main10 | **HEVC Rext 4:2:2** |
|---|---|---|---|---|
| **Electron 33**（Chromium 130） | 硬解 ✓ | 硬解 ✓ | 硬解 ✓ | **硬解 ✓** |
| **WebView2 / Edge 150** | 硬解 ✓ | 硬解 ✓ | 硬解 ✓ | **❌ 硬解软解都不支持** |

新版 Chromium 反而没有 Rext 支持。也就是说：**换 Tauri 会直接丢掉刚跨机验证过的 4:2:2 接收能力**
（1583 帧零错那条链路）。

**影响范围要说清楚，别夸大**：
- Windows **发送** 4:2:2 不受影响（走 ffmpeg/NVENC，不经 WebCodecs）
- Mac 本来就编不出 4:2:2，所以 **Mac→Win 方向本就是 4:2:0**，不受影响
- 真正丢的是 **Windows 作为接收端解 4:2:2** 的能力 → 影响 Win↔Win 高画质场景

### 三条可选路线（需用户拍板）

| 路线 | 体积 | 原生度 | 4:2:2 接收 | 工作量 |
|---|---|---|---|---|
| **A. 留在 Electron，只重做 UI** | 71 MB | 中（但 UI 可做到很原生） | ✅ 保留 | **小**——只改界面，管线不动 |
| **B. Tauri** | ~5 MB | 中高 | ❌ 丢失 | 中——外壳重写，管线复用 |
| **C. Rust + windows-rs 纯原生** | ~3 MB | 最高 | ✅（Media Foundation 自己解） | **大**——采集/编码/解码/渲染全部重写，跨机验证要重做 |

**我的看法**：用户否 Electron 的直接原因是「界面不像原生」，而**界面观感由设计决定、不由框架决定**——A 路线能立刻解决他真正抱怨的问题且零风险。B 拿 4:2:2 换 66MB 体积，划不划算取决于 Win↔Win 高画质是否重要。C 是终局形态但代价最大，适合确定长期投入时再做。

### ✅ 已定：走 A（留在 Electron）

用户拍板留在 Electron。副作用是**保住了 4:2:2 接收能力**——这是当前唯一能解 Rext 的引擎（见上表）。
**界面观感的问题交由设计稿解决，而不是换框架。**

## 七、当前状态（2026-07-22 冻结）

- **UI 一律等设计稿**：用户已委托专门的设计师统一重做两端界面，需求见 `docs/20-design-handoff.md`。
  两端都不再自行改 UI，避免继续 churn。
- 本文的**第三节（界面）作为交给设计师的功能性需求**参考——它定义了「有哪些状态、每个状态要表达什么」，
  视觉呈现由设计稿决定。
- **第四节（transport 变状态）已进协议 v1.9**（`HELLO.lanAddrs` + §3.5 连接升级），
  这部分与界面无关，可以先实现。

## 六、迁移顺序（确认后执行）

1. Tauri 骨架 + 上面三个状态的界面
2. 协议层直接搬（`protocol.js` 与会话逻辑是纯 JS，无 Electron 依赖）
3. 采集/编码：`desktopCapturer` → Tauri 侧用 `windows-capture` crate 或保留 getUserMedia
4. 托盘菜单：Tauri 原生托盘（比 Electron 的更贴近系统）
5. 打包验证 + 跨机回归（必须重跑真 4:2:2 那条链路）
